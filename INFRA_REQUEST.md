# インフラ構築 依頼書

流山青年会議所のイベント出欠管理アプリ(`attendance-app`)を Phase 2(本実装)に進めるにあたり、インフラ構築をお願いしたい依頼内容です。

アプリ開発側(村岡)とインフラ担当でスコープを分けて進めます。本ドキュメントは **依頼事項 + 求める条件 + 共有してほしい情報** をまとめたものです。実装手段は基本的にインフラ担当にお任せします。

---

## 1. プロジェクト概要

- **目的**: 流山青年会議所のイベント出欠管理・QR受付。月次例会・委員会活動・懇親会等で利用
- **想定ユーザー**: 流山JCメンバー **約50〜100名**
- **利用頻度**: 月に数回(月次例会 + 不定期イベント)。常時アクセスは多くない
- **使用デバイス**: スマートフォン主体(iOS Safari / Android Chrome)
- **現状**: HTML + JS 単一ファイルのプロトタイプを Vercel に静的ホスティング中
- **リポジトリ**: <https://github.com/ds-eishin-muraoka/attendance-app>

詳細仕様・データスキーマ・移行リスクは [PRODUCTION.md](./PRODUCTION.md) を参照。

---

## 2. 求めるゴール

Phase 2 完了時に、以下が動いている状態にしたい:

1. **独自ドメイン + HTTPS** でアプリが公開されている
2. ユーザーが **メールアドレス + メール認証** でログインできる
3. データが **マネージドDB(またはバックアップ運用可能なDB)** に保存されている
4. アプリの **デプロイが自動化** されている(GitHub main push → 自動デプロイ)
5. **障害時に復旧可能**(日次バックアップ等)
6. **月額コストが現実的な範囲**(目安: **$10 / 月 以下**を希望。マネージドDB分離など運用負荷を下げる選択をする場合は要相談、最大でも **$30 / 月** を超えない範囲で)

---

## 3. 想定構成(変更歓迎)

アプリ側で想定している構成は以下ですが、AWS経験豊富な担当の判断で **より良い構成があれば変更OK**(コスト・運用負荷の両面で検討してほしい)。

| 層 | 想定 | 備考 |
|---|---|---|
| ホスティング | **AWS Lightsail** | コスト重視。App Runner / EC2 / Elastic Beanstalk 等の代替提案歓迎 |
| バックエンド | **Node.js + TypeScript**(Fastify or Express) | ランタイム要件以外は任意 |
| DB | **PostgreSQL** | RDS / Lightsail同居 / Aurora Serverless v2 等、お任せ |
| 認証 | **Amazon Cognito** | ユーザープール + メール認証。代替提案(自前認証等)も可 |
| メール送信 | **Amazon SES** | 認証メール送信用。Sandbox→本番昇格申請必要 |
| フロント | **静的サイト**(将来 React + Vite) | 現状は Vercel ホスティング。AWS側に統合するなら CloudFront + S3 等 |
| DNS | **Route 53** または既存DNS | 任意 |

> 注: Vercel ホスティングを継続するか、AWS 側にフロントも寄せるかは要相談。フロントを AWS に寄せると認証 cookie の同一オリジン化が楽になる。

---

## 4. 依頼事項

### 4-1. AWS インフラ構築(必須)

- [ ] **既存AWSアカウント配下に本プロジェクト用の IAM / リソース分離**(プロジェクト識別タグや専用IAMロール等、運用ルールに沿って)
- [ ] **コンピュート**(Lightsail or 代替案)の立ち上げ + Node.js ランタイム導入
- [ ] **データベース**(PostgreSQL想定)構築 + 接続情報の安全な保管
- [ ] **ドメイン取得**(命名は任せる。`xxx-jc.example` 等、流山JC関連を想起できる名前を希望)
- [ ] **HTTPS化**(Let's Encrypt / ACM 等)
- [ ] **Amazon Cognito ユーザープール作成**(メール認証フロー)
- [ ] **Amazon SES 設定**(送信ドメイン認証 + Sandbox解除申請)
- [ ] **日次バックアップ**(DB / 設定ファイル)
- [ ] **CI/CD パイプライン**(GitHub main push → 自動デプロイ。GitHub Actions or CodePipeline 等任意)

### 4-2. 監視・運用(任意・将来でもよい)

- [ ] CloudWatch でのエラーログ・基本メトリクス可視化
- [ ] アラート(障害時に通知が飛ぶ仕組み。Slack or メール)
- [ ] WAF / セキュリティグループの基本設定

### 4-3. 開発支援(必須)

- [ ] アプリ開発者(村岡)に **デプロイ可能な開発者IAMユーザー** を発行
- [ ] **環境変数管理の仕組み**(Parameter Store / Secrets Manager 等)を整備し、開発者が値を書き込める権限を付与
- [ ] **ステージング環境の有無を相談**(なくてもよいが、あれば本番投入前の検証が楽)

---

## 5. 共有してほしい情報(構築後)

構築完了後、アプリ開発を進めるため以下を共有してほしい:

| カテゴリ | 共有してほしいもの |
|---|---|
| ドメイン | アプリのホスト名(例: `attend.xxx-jc.example`) |
| サーバ接続 | SSH接続情報 or デプロイトリガー方法 |
| DB | DB接続文字列(ホスト/ポート/DB名/読み書きユーザー)。パスワードは Secrets Manager 経由でアプリから参照させたい |
| Cognito | リージョン / User Pool ID / App Client ID / Hosted UI URL(使う場合) |
| SES | 送信元アドレス / リージョン / 制限緩和済みの本番モード状態 |
| デプロイ | GitHub Actions に登録すべきシークレット一覧(AWS_ACCESS_KEY_ID 等)or デプロイ手順 |
| ドキュメント | 構築した構成図 or リソース一覧(後の運用引き継ぎ用) |

開発側で **`.env.example`** や **接続用の TypeScript 型定義** を用意するので、必要な環境変数キー名はアプリ側で確定させて共有します。

---

## 6. アプリ開発側の担当範囲

インフラの責任範囲外として、開発側で以下をやります:

- バックエンドAPI実装(Node.js + TypeScript + Fastify想定)
- DBスキーマ定義 + マイグレーション(`prisma` or `drizzle` 等を想定)
- フロントエンド実装(現状の `web/index.html` を React + Vite に置換)
- Cognito を呼び出すフロント側の認証コード(SDK 利用)
- データ移行スクリプト(現プロトタイプの jsonbin.io データを新DBへ移送)
- アプリ層のエラーハンドリング・バリデーション

---

## 7. 制約・希望

- **コスト**: 月額 **$10 以下**(同居構成想定)を希望。マネージドDB分離等で運用負荷を下げる場合でも **$30 / 月** を上限としたい。それ以上になる場合は事前相談
- **リージョン**: 東京リージョン(`ap-northeast-1`)を希望
- **個人情報**: 氏名・メールアドレス・所属委員会程度。法定の個人情報保護対応(暗号化保存 + バックアップ暗号化)を依頼
- **可用性**: 24/365 高稼働は不要。月次例会の日中だけは確実に動いてほしい
- **将来拡張**: 流山JC以外の組織への横展開は **現時点では想定しないが、将来あり得る**。マルチテナント前提までは不要だが、スキーマ・命名で意識してくれると嬉しい

---

## 8. インフラ担当者から開発側へ確認したい事項(想定)

事前にこちらで答えられるものは以下:

| 想定質問 | 現時点の回答 |
|---|---|
| ユーザー数の上限は? | アクティブ 100名想定。ピーク同時接続は最大50程度 |
| データ量は? | 1イベントあたり 100名 × 数百バイト程度。年間数十イベントで合計数MB |
| 既存データ移行は必要? | 必要。jsonbin.io の JSON データを移送(開発側で対応) |
| メール送信頻度は? | 招待メール + 認証メール程度。月数百通レベル |
| ステージング環境は必要? | あれば嬉しいが、無くてもよい |
| アクセス制限(IP制限等)は? | 不要(JCメンバー外もアクセスURL知っていれば開ける状態だが、ログインで弾く) |

その他質問があれば随時相談。

---

## 9. 参考リンク

- [PRODUCTION.md](./PRODUCTION.md) — データスキーマ・移行リスク・AWS構成比較
- [CLAUDE.md](./CLAUDE.md) — 現状の機能一覧
- [リポジトリ](https://github.com/ds-eishin-muraoka/attendance-app)

---

## 10. 連絡先

- **アプリ開発担当**: 村岡(eishin-muraoka@dimage.co.jp)
- 不明点・質問は本リポジトリの Issue または直接メールで
